# MAXESS NITRO — SECTION 01 GUARDRAILS

Status: ACTIVE
Scope: Section-by-section execution

## Guardrail 01 — Section scope is absolute

A Section 01 task produces Section 01 only. The complete Results implementation is reference material, not permission to redesign or replace the page.

## Guardrail 02 — Reference is not authority

The previously supplied Orb/Bead artifact is a behavioral/design reference. It does not authorize returning to an old full-page renderer or treating a historical complete Results file as the production target.

## Guardrail 03 — One obvious Section 01 artifact

Future Section 01 work should start from `MAXESS-NITRO-SECTION-01.html` and this index rather than searching generic filenames in `docs/`.

## Guardrail 04 — Preserve validated Orb behavior

Unless a verified defect is demonstrated, preserve:

- score-dependent color;
- 6s Orb breathing;
- Orb composition;
- 14px / 220px / 10s desktop Bead orbit;
- 11px / 140px mobile Bead orbit;
- reduced-motion disabling Orb + Bead animation.

## Guardrail 05 — Naya is a human arrival, not a marketing banner

The opening hierarchy is:

Naya presence → Naya message → Listen → YOUR AI SCORE → score reveal → Orb/Bead → score context.

Use direct language. Avoid generic AI journey/potential language.

## Guardrail 06 — One Listen action

Never create a second audio system. The Section 01 CTA delegates to the existing listener when available and otherwise emits the established `maxess:naya-listen` event.

## Guardrail 07 — Verification state must be honest

Static implementation success does not equal visual QA, Groove publication, or live verification. If rendering or live evidence is unavailable, state the limitation explicitly.

## Guardrail 08 — Documentation discoverability

Section-specific execution documents should have explicit `MAXESS-NITRO-SECTION-01-...` names and be linked from `docs/MAXESS-NITRO-SECTION-01-INDEX.md`.

## Guardrail 09 — No silent overwrite

No automation lane may replace a verified Section 01 mutation from an obsolete baseline. Product ownership and write ownership must remain explicit.
