# SMART NOTES — RETRIEVAL INDEX

Canonical system: [`docs/SMART-NOTES.md`](../SMART-NOTES.md)

This index is the first retrieval surface for durable project learning. Search by **date, Entry ID, type, scope, or any keyword/alias**. Keywords are intentionally redundant so an AI does not need to remember the exact phrase used in the original conversation.

## Retrieval rules

- Start here before opening daily note files.
- Search natural-language concepts and aliases, not only exact titles.
- If a result looks relevant, open the linked daily note and read the full entry.
- Current repository authority always outranks historical memory.
- When a note is superseded, keep the old index row and point to the replacement.

## Entry index

| Entry ID | Date / Time | Type | Scope | Keywords / aliases | Summary | Note |
|---|---|---|---|---|---|---|
| SN-20260818-001 | 2026-08-18 15:55 PDT | architecture, process, memory | repository / AI execution | smart notes, memory, durable learning, conversation learning, timestamp, retrieval, keywords, AI context, notes system, daily notes | Smart Notes must be a structured durable-learning layer with timestamped entries, broad retrieval vocabulary, an index, and explicit capture/retrieval rules. | [`2026-08-18`](./2026-08-18.md) |

## Topic routing

### Repository / source of truth
Search: `repository`, `source of truth`, `authority`, `branch`, `baseline`, `canonical`, `current state`

### Memory / learning
Search: `smart notes`, `memory`, `durable learning`, `conversation learning`, `lesson`, `decision`, `discovery`, `timestamp`, `retrieval`

### QA / failures
Search: `failure`, `root cause`, `regression`, `guardrail`, `QA`, `verification`, `Oscar`, `retest`

### Product / UX
Search: `MAXESS`, `Results`, `Naya`, `Orb`, `dimensions`, `report`, `experience`, `mobile`, `accessibility`

### Deployment
Search: `Groove`, `live`, `deployment`, `public parity`, `release`

## Maintenance law

Every new durable note gets:

1. a stable Entry ID;
2. an ISO-8601 timestamp with timezone;
3. broad keywords and aliases;
4. a one-sentence summary;
5. an evidence pointer;
6. an explicit action or implication;
7. an index row.

Do not create orphan notes that are absent from this index.
