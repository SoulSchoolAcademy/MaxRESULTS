# 🔱 E01 Successor Torch

## Mission
Browser-verify the completed E01 Identity Doorway artifact and then prepare the production integration boundary without changing the architecture.

## Exact next actions

1. Check out `feat/nayanet-e01-identity-doorway-v2`.
2. Run `NAYANET/E01-IDENTITY-DOORWAY/tests/smoke-test.sh`.
3. Run a real browser pass at desktop and mobile widths.
4. Verify keyboard flow, focus visibility, reduced motion, name validation, Smart Name editing, reveal, Hub, MAXESS handoff, Naya Power video, all five challenge days, and the Day-felt interaction.
5. Verify no real-name exposure after the identity screen.
6. Run an Oscar visual review inside the intended iframe width.
7. Package the exact directory as a static ZIP and compute SHA-256.
8. Only after review approval, deploy the same artifact to Cloudflare Pages/static hosting.
9. Record the deployed URL as a separate evidence state.
10. Groove-verify only after the deployment is independently proven.

## Non-goals

Do not add authentication, Smart Mail, live communication, PSI writes, Smart Note writes, MAXESS scoring, media feeds, or Naya-to-Naya networking during this torch.

## Truth rule

A ZIP proves packaging. A local browser pass proves implementation behavior. A Cloudflare URL proves deployment only. A Groove embed proves integration only. Do not collapse these evidence states.
