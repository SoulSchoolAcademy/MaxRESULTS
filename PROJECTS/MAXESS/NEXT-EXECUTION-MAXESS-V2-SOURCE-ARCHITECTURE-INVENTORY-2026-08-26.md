# 🔱 NEXT EXECUTION — MAXESS V2 SOURCE + ARCHITECTURE INVENTORY

**Project:** MAXESS  
**Directive:** `PROJECTS/MAXESS/MAXESS-MASTER-ENGINEERING-DESIGN-DIRECTIVE-V2.md`  
**Priority:** P0 / ARCHITECTURE FIRST  
**Status:** READY TO EXECUTE

## Mission

Stop isolated E00 patching. Determine the best unified implementation from the complete source lineage, then build toward one authoritative MAXESS assessment machine and one deterministic Results contract.

## Execution command

### 1. READ AUTHORITY

Read the current MAXESS project hub and V2 directive first, then the relevant Naya Power engineering/design/Smart Note laws.

### 2. INVENTORY ASSESSMENT SOURCES

Inspect:

- `E00 796`
- `E00 700`
- `E00 1800`
- `E00.01`
- `E00.02`
- `E00.03`

Record for each:

- state model;
- question data;
- answer data;
- scoring logic;
- persistence;
- Continue handling;
- completion handling;
- result construction;
- result validation;
- result release;
- bridges;
- timing dependencies;
- diagnostics;
- dead/duplicate logic;
- proven behavior;
- known failure modes.

### 3. INVENTORY RESULTS SOURCES

Inspect:

- `E01` through `E09`;
- `E01-SECTION-01-WORKING.html`;
- result consumer artifacts;
- integration documentation;
- bridge/hydration workflows.

For each E-section record:

**purpose → inputs → derived display data → forbidden authority → dependencies → current quality → reuse plan.**

### 4. RECONCILE THE RESULT CONTRACT

Determine the smallest complete `MAXESS_RESULT_V1` contract that satisfies all nine canonical Results sections without duplication.

Verify:

- overall score;
- mastery band;
- five dimensions;
- dimension scores;
- responses;
- fingerprint;
- strongest/opportunity dimensions;
- selected interests where applicable;
- Naya metadata;
- audio metadata;
- version/integrity metadata.

### 5. RECONCILE SCORING

Trace one canonical path from answer value → dimension → raw total → normalized score → mastery band.

Prove the 15 × 0–4 model mathematically.

Identify every competing scorer and mark it for removal or conversion to presentation-only logic.

### 6. RECONCILE COMPLETION

Trace Q1 → Q15 and prove that exactly one completion path exists.

Explicitly remove reliance on:

- timing races;
- DOM scraping;
- polling;
- duplicate listeners;
- bridge choreography used only to compensate for missing authority.

### 7. DESIGN THE UNIFIED E00

Produce a concrete implementation map for:

```text
TOPIC / ASSESSMENT DEFINITION
        ↓
STATE MACHINE
        ↓
QUESTION RENDERER
        ↓
ANSWER CAPTURE
        ↓
RESPONSE STORE
        ↓
SCORING ENGINE
        ↓
RESULT VALIDATOR
        ↓
IMMUTABLE MAXESS_RESULT_V1
        ↓
RESULTS RELEASE
```

### 8. GOLDEN TEST PLAN

Define automated and live tests that prove AI Score is reproduced exactly before dynamic-topic expansion is considered green.

### 9. DYNAMIC FOUNDATION

Define the minimum deterministic compiler interfaces required for:

`TOPIC → COVERAGE → KNOWLEDGE → OBJECTIVES → DIMENSIONS → ARCHETYPES → RUBRIC → QUESTIONS.`

Do not overbuild runtime generation before the canonical AI Score path is stable.

### 10. DESIGN / PERFORMANCE INVENTORY

Identify the best existing visual work from the source artifacts, especially jewel controls, Naya presence, motion, typography, spacing, and responsive behavior. Preserve good ideas; reimplement fragile ones.

## Required deliverable

Produce one source/architecture matrix and one authoritative rebuild map. Then implement the highest-leverage coherent changes that can be safely verified in the current execution.

## Completion gate for this execution

Do not call the application green yet unless the evidence proves the corresponding behavior. The expected outcome of this execution is a **verified architecture/source map plus the first coherent implementation changes**, not a premature “done.”

## Required final communication

Return:

1. current truth;
2. what was inspected;
3. what was learned;
4. what was changed;
5. tests run;
6. observed results;
7. green/yellow/red status;
8. evidence/receipts;
9. remaining blockers;
10. next execution.

**Do not ask for permission to perform clearly in-scope, safe, evidence-backed work. Lead.**
